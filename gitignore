# Data (downloaded via scripts/download_data.py — not committed)
data/
*.csv
*.data

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg

# Keep compiled monitoring report PDFs (override the wildcard rules above)
!docs/monitoring/*.pdf

# Kaggle credentials — never commit
kaggle.json

# OS / editor
.DS_Store
.vscode/
