# Core
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12

# Market data / VaR / Monte Carlo / portfolio dashboard
yfinance>=0.2

# Macro data (stress testing, liquidity risk calibration)
pandas_datareader>=0.10

# Credit risk / PD model
scikit-learn>=1.3
statsmodels>=0.14

# Kaggle dataset downloads (optional — only needed if using download_data.py's
# Kaggle functions; requires ~/.kaggle/kaggle.json credentials)
kaggle>=1.6

# Notebooks (if using Jupyter in VS Code)
jupyter>=1.0
ipykernel>=6.25

# Dashboard (portfolio risk dashboard project)
streamlit>=1.28
